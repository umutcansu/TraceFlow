<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />

  <application>
    <provider
      android:name="io.github.umutcansu.traceflow.TraceFlowInitProvider"
      android:authorities="${applicationId}.traceflow-init"
      android:exported="false" />
  </application>

</manifest>
